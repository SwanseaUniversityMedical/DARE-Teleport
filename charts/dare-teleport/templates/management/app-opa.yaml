{{- if .Values.appOpa.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-opa
  namespace: {{ .Values.global.mgmt.argocd.namespace }}
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  destination:
    namespace: opa
    server: {{ .Values.global.mgmt.address }}
  project: {{ .Values.global.mgmt.argocd.project }}
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:  
    - CreateNamespace=true
  source:
    repoURL: {{ default .Values.global.repoURL .Values.appOpa.source.repoURL }}
    chart: {{ .Values.appOpa.source.chart }}
    targetRevision: {{ .Values.appOpa.source.targetRevision }}

    helm:
      valueFiles:
        - values.yaml
      {{- if .Values.appOpa.values }}
      values: |
        {{- .Values.appOpa.values | toYaml | nindent 8 }}
      {{- end }}
{{- end }}